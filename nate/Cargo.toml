[package]
name = "nate"
version = "0.1.0"
edition = "2018"
authors = ["René Kijewski <rene.[SURNAME]@fu-berlin.de>"]
license = "ISC OR MIT OR Apache-2.0"
description = "Not a Template Engine. Derive Display using a template."
homepage = "https://github.com/kijewski/nate"
repository = "https://github.com/kijewski/nate"
categories = ["template-engine"]
keywords = ["html", "template"]
readme = "README.md"

[lib]
name = "nate"

[dependencies]
nate-common = { path = "../nate-common", version = "0.1.0" }
nate-derive = { path = "../nate-derive", version = "0.1.0" }
